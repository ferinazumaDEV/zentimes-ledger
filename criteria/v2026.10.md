# Criteria v2026.10 — 2026-09-23

The rule set the inspector printed on 2026-09-23, captured at 14:44Z from
https://zentimes.es/tools/ai-inspector/?url=https://zentimes.es/ (EN) and
https://zentimes.es/es/herramientas/inspector-ia/?url=https://zentimes.es/ (ES), with the two landing pages
captured in the same minute. Rules are quoted verbatim in both languages; the "measured on the home page" values
are what that run printed. Unlike the v2026.09.2 record, the printed commands were **not** re-run for this
file: the values below are the report's, read from the served HTML and the visible-text captures, nothing else.

History entry for this version, as printed (EN, then ES):

> **v2026.10 · 2026-09-23** — Closes the gaps that let incomplete pages score "excellent": indexing (noindex,
> nosnippet, X-Robots-Tag, which overrides everything), viewport, dates, article authorship, minimum properties
> per structured-data type, broken outbound links and figures with a source. The criteria now carry a review
> date: if nobody reviews them within 45 days, deployment fails.

> **v2026.10 · 2026-09-23** — Se cierran los huecos que dejaban sacar «excelente» a páginas incompletas:
> indexación (noindex, nosnippet, X-Robots-Tag, que manda sobre todo), viewport, fechas, autoría de artículos,
> propiedades mínimas por tipo de datos estructurados, enlaces salientes rotos y cifras con fuente. Los criterios
> pasan a tener fecha de revisión: si nadie los revisa en 45 días, el despliegue falla.

Version/date line on the report: `Criteria v2026.10 · 2026-09-23` (ES: `Criterios v2026.10 · 2026-09-23`). On
the landing page: `Criteria v2026.10` / `Criterios v2026.10`.

The "review date" the entry mentions is not printed anywhere on the page: the served HTML carries no element
with that date, only the sentence in the history. What the deployment does with it is not observable from
outside and is not claimed here.

Twenty-four cards. Twenty-one carry a label; three are informational. Seven cards are new versus v2026.09.2
(`indexacion`, `viewport`, `fechas`, `autoria`, `completitud`, `salientes`, `cifras`); the seventeen others
print the same rule text as v2026.09.2 with the version token changed to `v2026.10`. Labels grade preconditions
of legibility. The page states, and this record repeats, that no check measures whether an AI cites the page.
The cookbook the four linked cards are cross-checked against (v0.1.4) grades every engine effect as
`experimental`; the cross-check line still reads `2026-09-23 (13:32Z)`, i.e. it was not re-run for v2026.10.

Fields per card, in the order the page prints them: **rhythm · reason** → **rule** → **evidence class ·
version** → caveat (if any) → cookbook line (if any) → command.

---

## 1. HTTP status and time to response — `status`

Stable · HTTP semantics · `technical-seo` · v2026.10

> Excellent = 200 with no redirect, over HTTPS · good = 200 after 1 redirect · low = 2 or more redirects, or no
> HTTPS · none = no 200. Transport security (HSTS) is rated separately.

> Excelente = 200 sin redirigir y por HTTPS · buena = 200 tras 1 redirección · baja = 2 o más redirecciones, o
> sin HTTPS · nula = no responde 200. La seguridad del transporte (HSTS) se valora aparte.

Caveat: "The time is one measurement from one place and is not rated: network timing varies with where it is
measured from."

```sh
curl -sIL 'https://zentimes.es/' | grep -iE '^(HTTP/|location:)'
```

Measured on the home page: **Excellent** · `200 · 22 ms` (EN run) / `200 · 8 ms` (ES run) · "200 direct."

## 2. Transport security (HTTPS and HSTS) — `transporte`

Stable · RFC 6797 · `security` · v2026.10

> Security, not legibility. Excellent = HTTPS with HSTS of at least one year and includeSubDomains · strong =
> HSTS of at least one year, no includeSubDomains · good = HSTS under one year · low = HTTPS without HSTS · none =
> no HTTPS. One year is the browsers' preload-list minimum.

> Seguridad, no legibilidad. Excelente = HTTPS con HSTS de al menos un año e includeSubDomains · notable = HSTS
> de al menos un año, sin includeSubDomains · buena = HSTS de menos de un año · baja = HTTPS sin HSTS · nula = sin
> HTTPS. El año es el mínimo de la lista de precarga de los navegadores.

```sh
curl -sI 'https://zentimes.es/' | grep -i '^strict-transport-security:'
```

Measured on the home page: **Strong / Notable** · `max-age=31536000` · "HSTS max-age=31536000." How to improve
it, as printed: "Add the header Strict-Transport-Security: max-age=31536000; includeSubDomains. It forces the
browser to always use HTTPS with your domain."

`security` remains the inspector's own class, not one of the three `evidence_class` values of cookbook v0.1.4.

## 3. Indexing directives (robots meta, X-Robots-Tag) — `indexacion`

**New in v2026.10.** Changing · how each engine applies its directives to AI keeps changing · `technical-seo` ·
v2026.10

> Directives from <meta name="robots"> and the X-Robots-Tag header. Excellent = none that restrict · low =
> nosnippet or max-snippet:0 (Google applies them to its AI features too: the text cannot be used to answer) ·
> none = noindex or none (the page asks not to be indexed, which overrides everything else).

> Directivas de <meta name="robots"> y de la cabecera X-Robots-Tag. Excelente = ninguna que limite · baja =
> nosnippet o max-snippet:0 (Google las aplica también a sus funciones de IA: el texto no se puede usar para
> responder) · nula = noindex o none (la página pide no ser indexada, y eso anula todo lo demás).

Three steps only (excellent, low, none): no strong or good outcome is defined for this card.

```sh
curl -sI 'https://zentimes.es/' | grep -i '^x-robots-tag:'; curl -s 'https://zentimes.es/' | grep -oiE '<meta[^>]+name="(robots|googlebot)"[^>]*>'
```

Measured on the home page: **Excellent** · `—` · "No restrictive directives."

## 4. Mobile viewport — `viewport`

**New in v2026.10.** Stable · HTML and WCAG 1.4.4 · `technical-seo` · v2026.10

> Excellent = <meta name="viewport"> with width=device-width and zoom not blocked · strong = width=device-width
> but zoom blocked (user-scalable=no or maximum-scale=1; WCAG 1.4.4) · good = viewport without
> width=device-width · none = no viewport.

> Excelente = <meta name="viewport"> con width=device-width y sin bloquear el zoom · notable = width=device-width
> pero bloquea el zoom (user-scalable=no o maximum-scale=1; WCAG 1.4.4) · buena = viewport sin
> width=device-width · nula = sin viewport.

```sh
curl -s 'https://zentimes.es/' | grep -oiE '<meta[^>]+name="viewport"[^>]*>'
```

Measured on the home page: **Excellent** · `width=device-width, initial-scale=1` · "viewport:
width=device-width, initial-scale=1" (the "why" line is printed without a final full stop on both pages).

## 5. Title and meta description — `titulo-descripcion`

Stable · a search convention for years · `technical-seo` · v2026.10

> Excellent = title of 30–60 characters, description of 120–160, and og:title and og:description declared ·
> strong = lengths right, Open Graph incomplete · good = both, one outside length · low = only one · none =
> neither. The lengths are a convention (what fits uncut in a result), not evidence.

> Excelente = título de 30 a 60 caracteres, descripción de 120 a 160, y og:title y og:description declarados ·
> notable = longitudes bien, sin Open Graph completo · buena = los dos, alguno fuera de longitud · baja = solo
> uno · nula = ninguno. Las longitudes son una convención (lo que cabe sin cortarse en un resultado), no
> evidencia.

```sh
curl -s 'https://zentimes.es/' | grep -oE '<title[^>]*>[^<]*</title>|<meta[^>]+(name="description"|property="og:(title|description)")[^>]*>'
```

Measured on the home page: **Good / Buena** · "Title: 37 characters · description: 252. Missing: a 120–160
description (it has 252)." How to improve it, as printed: "A 30–60 character <title> saying what the page is, a
120–160 character <meta name="description"> summing it up, and their twins og:title and og:description, which
previews and many automated readers use."

## 6. Declared language (`<html lang>`) — `idioma`

Stable · HTML Standard · `technical-seo` · v2026.10

> Excellent = valid <html lang> and og:locale declared with the same language · strong = valid <html lang>, no
> matching og:locale · low = malformed · none = not declared.

> Excelente = <html lang> válido y og:locale declarado con el mismo idioma · notable = <html lang> válido, sin
> og:locale coherente · baja = mal formado · nula = sin declarar.

```sh
curl -s 'https://zentimes.es/' | grep -oE '<html[^>]*>|<meta[^>]+property="og:locale"[^>]*>'
```

Measured on the home page: **Excellent** · `en` · "lang="en" · og:locale: en_US."

## 7. Words present without running JavaScript — `sin-js`

Changing · depends on whether AI crawlers run JavaScript · `technical-seo` · v2026.10

> Only the clear case is flagged: none = under 50 words without running JavaScript, the page arrives nearly
> empty. Above that the number is reported unrated: no data ties a word count to an outcome, so tightening it
> would be made up.

> Solo se marca el caso claro: nula = menos de 50 palabras sin ejecutar JavaScript, la página llega casi vacía.
> Por encima se informa del número sin valorar: ningún dato liga una cantidad de palabras a un resultado, así
> que endurecerlo sería inventar.

Caveat: "This is what a client that does not execute scripts receives. A low number next to a full-looking page
means the content arrives by JavaScript."

Cookbook line: `words_visible_no_js` · `04-technical/ssr-vs-csr-rendering` · cross-checked 2026-09-23 (13:32Z)
against cookbook v0.1.4. **Same metric id as the cookbook.**

```sh
curl -s 'https://zentimes.es/' | perl -0777 -pe 's/<script.*?<\/script>//gis; s/<style.*?<\/style>//gis; s/<!--.*?-->//gis; s/<[^>]+>/ /g; s/&#?[a-z0-9]+;/ /gi; s/\s+/ /g; s/^\s+|\s+$//g' | wc -w
```

Measured on the home page: **Not rated / Sin valorar** (`data-level="sin-evaluar"`) · `526` · "526 words."

## 8. JSON-LD nodes carrying an @type (nested included) — `jsonld-nodos`

Stable · Schema.org vocabulary · `aeo` · v2026.10

> Excellent = no invalid blocks; your entity (Organization, Person, LocalBusiness…) with @id, name, url and logo
> or image; a WebSite and a WebPage; and the graph linked by @id (some node references your entity) · strong =
> entity and site declared, one of those details missing · good = types present, but not who you are · low =
> only invalid blocks · none = no structured data.

> Excelente = ningún bloque inválido; tu entidad (Organization, Person, LocalBusiness…) con @id, name, url y
> logo o image; un WebSite y un WebPage; y el grafo conectado por @id (algún nodo referencia a tu entidad) ·
> notable = entidad y sitio declarados, falta alguno de esos detalles · buena = hay tipos, pero no quién eres ·
> baja = solo bloques inválidos · nula = sin datos estructurados.

Caveat: "Counting nodes is not validating them. This says how many things declare a type, not whether the type
is the right one."

Cookbook line: `typed_entities` · `04-technical/structured-data-jsonld` · cross-checked 2026-09-23 (13:32Z)
against cookbook v0.1.4. **Same metric id as the cookbook**; the label is decided by the graph rule above.

```sh
curl -s 'https://zentimes.es/' | grep -o '"@type":' | wc -l
```

Measured on the home page: **Strong / Notable** · `9` · "9 nodes. Missing: name, url and logo or image on the
entity and a WebPage." How to improve it, as printed: "A JSON-LD graph with your entity (stable @id, name, url
and logo), plus a WebSite and a WebPage that reference it by its @id. It is the difference between declaring
loose things and describing who you are and what you publish."

## 9. Distinct Schema.org types present — `jsonld-tipos`

Informational: no rule, no rhythm, no label.

```sh
curl -s 'https://zentimes.es/' | grep -o '"@type":"[^"]*"' | sort -u | wc -l
```

Measured on the home page: `Person, PostalAddress, ProfessionalService, Service, WebSite` (`data-value="5"`).
The card displays the names, the printed command prints the count.

## 10. Publication and update dates — `fechas`

**New in v2026.10.** Changing · how much freshness weighs for each engine is being measured ·
`geo-precondition` · v2026.10

> Page dates in structured data (dateModified/datePublished) or article:modified_time. Excellent = a valid,
> non-future dateModified · strong = datePublished only · low = malformed or future dates · none = no dates. An
> update date is how a machine knows whether what it reads is still current.

> Fechas de la página en los datos estructurados (dateModified/datePublished) o en article:modified_time.
> Excelente = dateModified válida y no futura · notable = solo datePublished · baja = fechas mal formadas o en el
> futuro · nula = sin fechas. Una fecha de actualización es cómo una máquina sabe si lo que lee sigue vigente.

```sh
curl -s 'https://zentimes.es/' | grep -oE '"(datePublished|dateModified)":"[^"]*"|<meta[^>]+property="article:(published|modified)_time"[^>]*>'
```

Measured on the home page: **None / Nula** · `— · —` · "No dates." How to improve it, as printed: "Declare
dateModified (and datePublished) in ISO 8601 on the page node, and update it when the content really changes,
not on every deploy." This is the only **None** label on the site's own home page in this run.

## 11. Article authorship — `autoria`

**New in v2026.10.** Changing · how engines use authorship is still being studied · `geo-precondition` ·
v2026.10

> Articles only (Article, BlogPosting, NewsArticle…). Excellent = author is a Person or Organization with name,
> url and sameAs · strong = with name and url · good = name only · none = article with no author. No article on
> the page: not applicable.

> Solo en artículos (Article, BlogPosting, NewsArticle…). Excelente = author es una Person u Organization con
> nombre, url y sameAs · notable = con nombre y url · buena = solo nombre · nula = artículo sin autor. Sin
> artículo en la página: no aplica.

```sh
curl -s 'https://zentimes.es/' | grep -oE '"author":[^]}]*'
```

Measured on the home page: **Not rated / Sin valorar** (`data-level="sin-evaluar"`) · `—` · "No article on the
page." The rule's "not applicable" outcome is printed here as the `sin-evaluar` label, not as plain text.

## 12. Structured data completeness — `completitud`

**New in v2026.10.** Changing · each type's minimums change with search engines' documentation · `aeo` ·
v2026.10

> Every node of a known type carries its minimum properties (e.g. Article = headline, datePublished, author and
> image; Service = name and provider; Organization = name and url). Excellent = all complete · strong = 1
> incomplete · low = 2 or more · no known types: not applicable. The minimum list is a convention based on
> Google's and schema.org's documentation.

> Cada nodo de un tipo conocido lleva sus propiedades mínimas (por ejemplo: Article = headline, datePublished,
> author e image; Service = name y provider; Organization = name y url). Excelente = todos completos · notable =
> 1 incompleto · baja = 2 o más · sin tipos conocidos: no aplica. La lista de mínimos es una convención basada en
> la documentación de Google y schema.org.

Caveat (the full list of minimums, as printed): "Minimums checked: Article (headline, datePublished, author,
image) · BlogPosting (headline, datePublished, author, image) · NewsArticle (headline, datePublished, author,
image) · Organization (name, url) · ProfessionalService (name, url) · LocalBusiness (name, address) · Person
(name) · WebSite (name, url) · WebPage (url) · BreadcrumbList (itemListElement) · FAQPage (mainEntity) ·
Question (name, acceptedAnswer) · Service (name, provider) · Product (name) · Event (name, startDate, location)
· DefinedTerm (name, description)."

Own rule, no cookbook id. Sixteen types have a minimum list; a node of any other type is printed as "without a
minimum list" and not rated.

```sh
curl -s 'https://zentimes.es/' | grep -oE '<script[^>]+application/ld\+json[^>]*>.*</script>' | head -c 4000
```

Measured on the home page: **Excellent** · `7/7 complete · 9 typed nodes, 2 without a minimum list` · "7 of 7
complete. 2 of 9 nodes are of types with no minimum list (PostalAddress) and are not rated." The card's HTML
attribute reads `data-value="0"`; what it counts is not published, so it is recorded as seen and not
interpreted.

## 13. Your entity's identity links (sameAs) — `entidad`

Changing · how engines use entities is still being studied · `geo-precondition` · v2026.10

> Excellent = every checkable sameAs responds, at least one could actually be checked, all use HTTPS and none is
> repeated · strong = they respond, but one of those details fails · low = some return 404/410, or the entity
> has no sameAs · none = no entity. Links that block bots (403, 429, 999) count as uncheckable, not broken. The
> first 8 are checked, for responding, not for being the same entity.

> Excelente = todos los sameAs comprobables responden, al menos uno se ha podido comprobar, todos van por HTTPS
> y ninguno está repetido · notable = responden, pero falla alguno de esos detalles · baja = alguno da 404/410, o
> la entidad no tiene sameAs · nula = no hay entidad. Los que bloquean a los bots (403, 429, 999) cuentan como no
> comprobables, no como rotos. Se comprueban los 8 primeros, y que respondan, no que sean de la misma entidad.

Own rule (not the cookbook's `entities_resolved`).

```sh
curl -s 'https://zentimes.es/' | grep -oE '"sameAs":(\[[^]]*\]|"[^"]*")'
```

Measured on the home page: **Excellent** · two sameAs URLs (the public GitHub and LinkedIn profiles declared on
the site; `data-value="2"`) · "2 sameAs: 1 respond, 0 missing, 1 uncheckable."

## 14. Declared canonical URL — `canonical`

Stable · RFC 6596 · `technical-seo` · v2026.10

> Excellent = a single absolute HTTPS canonical, no query string, pointing to this page and matching og:url ·
> strong = self-referencing, but one of those details fails · good = absolute to another URL (may be intended) ·
> low = relative, or more than one · none = no canonical.

> Excelente = una sola canónica, absoluta, por HTTPS, sin parámetros, que apunta a esta página y coincide con
> og:url · notable = apunta a sí misma, pero falla alguno de esos detalles · buena = absoluta a otra URL (puede
> ser intencionado) · baja = relativa o más de una · nula = sin canónica.

```sh
curl -s 'https://zentimes.es/' | grep -oE '<link[^>]*rel="canonical"[^>]*>|<meta[^>]+property="og:url"[^>]*>'
```

Measured on the home page: **Excellent** · `https://zentimes.es/` · "Points to this same page."

## 15. hreflang values declared — `hreflang`

Stable · a search convention for years · `technical-seo` · v2026.10

> Rated only if the page declares hreflang. Excellent = x-default, self-reference, absolute URLs, and every
> language version links back to this one (Google ignores pairs without a return link; up to 5 checked) · strong
> = all that except the return links · good = missing x-default or self-reference · low = malformed values. No
> hreflang: does not apply to a single-language site.

> Solo se valora si la página declara hreflang. Excelente = x-default, autorreferencia, URLs absolutas y cada
> versión de idioma enlaza de vuelta a esta (Google ignora los pares sin retorno; se comprueban hasta 5) ·
> notable = todo eso salvo el retorno · buena = falta x-default o la autorreferencia · baja = valores mal
> formados. Sin hreflang: no aplica a una web de un solo idioma.

```sh
curl -s 'https://zentimes.es/' | grep -oE '<link[^>]*hreflang="[^"]*"[^>]*>'
```

Measured on the home page: **Excellent** · `en, es, x-default` · "3 values."

## 16. Heading structure — `encabezados`

Changing · each engine chunks its own way, and that is being measured · `geo-precondition` · v2026.10

> Excellent = one h1, 2 or more h2, no skipped levels (an h3 with no h2 before it, an h4 with no h3…) and no
> empty headings · strong = h1 and h2 right, but with skips or empties · good = one h1 and one h2 · low =
> several h1, or no h2 · none = no h1. A convention: same class as the cookbook's chunking recipe, not its
> metric.

> Excelente = un solo h1, 2 h2 o más, ningún salto de nivel (un h3 sin h2 antes, un h4 sin h3…) y ningún
> encabezado vacío · notable = h1 y h2 bien, pero con saltos o vacíos · buena = un h1 y un h2 · baja = varios h1,
> o ningún h2 · nula = sin h1. Es una convención: comparte clase con la receta de troceado del cookbook, pero no
> es su métrica.

```sh
curl -s 'https://zentimes.es/' | perl -0777 -pe 's/<script.*?<\/script>//gis; s/<!--.*?-->//gs' | grep -oiE '<h[1-6][ >]' | tr -d '< >'
```

Measured on the home page: **Excellent** · `h1: 1 · h2: 5` · "1 h1 · 5 h2."

## 17. Links to sections of this page (#id) — `anclas`

Stable · HTML Standard · `geo-precondition` · v2026.10

> Links on this page to one of its own sections (#id). Excellent = all reach an id that exists exactly once, and
> no id is repeated anywhere on the page (HTML requires it) · strong = links resolve, but some ids are repeated ·
> low = some lead nowhere. No such links: not applicable. Same rule as the cookbook's
> unresolved_fragment_links, applied to the whole page.

> Enlaces de esta página a una sección suya (#id). Excelente = todos llevan a un id que existe una sola vez y
> además no hay ningún id repetido en toda la página (el HTML lo exige) · notable = los enlaces resuelven, pero
> hay ids repetidos · baja = alguno no lleva a ningún sitio. Sin enlaces así: no aplica. Misma regla que
> unresolved_fragment_links del cookbook, aplicada a toda la página.

`needs-verification` carried over from v2026.09.2: `unresolved_fragment_links` is not in cookbook v0.1.4 (it is
the check proposed in cookbook pull request #32). Whether that PR has been merged and tagged since was not
checked for this file.

```sh
curl -s 'https://zentimes.es/' | grep -oE 'href="#[^"]*"' | sort -u
```

Measured on the home page: **Excellent** · `1` · "1 links, all resolve, no repeated ids." HTML attribute
`data-value="0"` while the display reads `1`, as in v2026.09.2.

## 18. Image alt text — `imagenes`

Stable · WCAG 1.1.1 · `technical-seo` · v2026.10

> Excellent = every image has alt and none is a file name (alt="" is fine for decorative ones) · strong = 90% or
> more · good = 70% or more · low = under 70% · none = none. No images: not applicable. The percentages are a
> convention; the underlying rule is WCAG 1.1.1.

> Excelente = todas las imágenes llevan alt y ninguno es un nombre de fichero (alt="" vale para las decorativas)
> · notable = 90 % o más · buena = 70 % o más · baja = menos del 70 % · nula = ninguna. Sin imágenes: no aplica.
> Los porcentajes son una convención; la regla de fondo es WCAG 1.1.1.

```sh
curl -s 'https://zentimes.es/' | grep -oiE '<img[^>]*>'
```

Measured on the home page: **Excellent** · `1/1 with alt` · "1 of 1 with a useful alt (100%)." The EN value
string is now English (v2026.09.2 printed `1/1 con alt` on the EN page).

## 19. Outbound links (other domains) — `salientes`

**New in v2026.10.** Stable · HTTP semantics · `technical-seo` · v2026.10

> Links from this page to other domains (up to 10 checked). Excellent = at least one checkable and none returns
> 404/410 · low = some return 404/410 · no external links, or none checkable: not rated. Links that block bots
> (403, 429, 999) count as uncheckable.

> Enlaces de esta página a otros dominios (se comprueban hasta 10). Excelente = al menos uno comprobable y
> ninguno da 404/410 · baja = alguno da 404/410 · sin enlaces externos, o ninguno comprobable: no se valora. Los
> que bloquean a los bots (403, 429, 999) cuentan como no comprobables.

Own rule, no cookbook id. Two steps only (excellent, low) plus a "not rated" outcome.

```sh
curl -s 'https://zentimes.es/' | grep -oE 'href="https?://[^"]*"' | sort -u
```

Measured on the home page: **Excellent** · `3` · "3 external links · 3 checked: 2 respond, 0 broken, 1
uncheckable. Uncheckable: <the LinkedIn profile URL declared on the site>." (The page prints that URL in full;
this file does not repeat it.) HTML attribute `data-value="0"`; what it counts is not published.

## 20. Figures with a source — `cifras`

**New in v2026.10.** Changing · how figures are attributed in answers is being measured · `geo-precondition` ·
v2026.10

> Paragraphs and list items containing a percentage. Excellent = all carry a linkable source: an http(s) link in
> the same paragraph or in its containing block, such as a card (any domain, as in the anchoring recipe), or a
> footnote (#anchor) whose target holds such a link · strong = 80% or more · good = 50% or more · low = less · no
> percentages: not applicable. The cookbook's anchoring idea (the source next to the claim) applied to figures;
> the percentages are a convention.

> Párrafos y elementos de lista con un porcentaje. Excelente = todos llevan su fuente enlazable: un enlace
> http(s) en el mismo párrafo o en el bloque que lo contiene, como una tarjeta (de cualquier dominio, como en la
> receta de anclaje), o una nota al pie (#ancla) cuyo destino lleva ese enlace · notable = 80 % o más · buena =
> 50 % o más · baja = menos · sin porcentajes: no aplica. Es la idea de la receta de anclaje del cookbook (la
> fuente al lado de la afirmación) aplicada a las cifras; los porcentajes son una convención.

Same idea, not same metric: the rule names "the cookbook's anchoring idea" and "the anchoring recipe" but prints
no metric id and no recipe path, and the card carries no `data-metric`. Which cookbook recipe is meant, and
whether v0.1.4 has a metric for it: `needs-verification` (not checked for this file).

```sh
curl -s 'https://zentimes.es/' | grep -oE '[0-9]+([.,][0-9]+)? ?%'
```

Measured on the home page: **Not rated / Sin valorar** (`data-level="sin-evaluar"`) · `—` · "No percentages in
the text."

## 21. robots.txt — AI agents — `robots`

Informational: no rhythm line, no label. The caveat is the whole text:

> The recipe's eight tokens, for the root path (/). Most of them are training or usage tokens: blocking them is
> your decision, not a defect, so this card is not rated. Google-Extended governs training and grounding only —
> it does not affect Google Search.

> Los ocho de la receta, para la raíz (/). La mayoría son de entrenamiento o de uso: bloquearlos es decisión
> tuya, no un defecto, así que esta tarjeta no se valora. Google-Extended gobierna solo entrenamiento y grounding
> — no afecta a la Búsqueda de Google.

Cookbook line: `ai_user_agents_allowed` · `04-technical/ai-crawler-access` · cross-checked 2026-09-23 (13:32Z)
against cookbook v0.1.4. **Same metric id as the cookbook.**

```sh
curl -sf 'https://zentimes.es/robots.txt' | python3 -c 'import sys,re
G={};C=[];S=0
for l in sys.stdin:
 l=l.split("#")[0].strip()
 if ":" not in l: continue
 f,_,v=l.partition(":");f=f.strip().lower();v=v.strip()
 if f=="user-agent":
  C=C if S else [];C.append(v.lower());G.setdefault(v.lower(),[]);S=1
 elif f in("allow","disallow"):
  S=0
  for a in C:G[a].append((f,v))
def m(p):
 e=p.endswith("$");b=p[:-1] if e else p
 return re.match("".join(".*" if c=="*" else re.escape(c) for c in b)+("$" if e else ""),"/")
def ok(u):
 R=G.get(u.lower(),G.get("*"))
 if R is None:return 1
 L,A=-1,1
 for d,v in R:
  if v=="":
   if d!="disallow":continue
   n,x=0,1
  elif m(v):n,x=len(v),d=="allow"
  else:continue
  if n>L or(n==L and x):L,A=n,x
 return A
print(sum(ok(u) for u in "GPTBot ClaudeBot anthropic-ai PerplexityBot Google-Extended CCBot Applebot-Extended Bytespider".split()))'
```

Measured on the home page: `8/8 · GPTBot: ✓ · ClaudeBot: ✓ · anthropic-ai: ✓ · PerplexityBot: ✓ ·
Google-Extended: ✓ · CCBot: ✓ · Applebot-Extended: ✓ · Bytespider: ✓` (`data-value="8"`).

## 22. robots.txt — assistants that search to answer — `robots-busqueda`

Changing · the rule is RFC 9309, but the agent list changes every few months · `technical-seo` · v2026.10

> For the root (/), only the agents that search to answer (OAI-SearchBot, ChatGPT-User, PerplexityBot,
> Perplexity-User). Excellent = a robots.txt exists and all 4 may read · strong = they may, but only because
> there is no robots.txt (policy by omission; a convention) · low = some · none = none. Training crawlers do not
> count here: blocking them is a decision, not a defect.

> Sobre la raíz (/), solo los agentes que buscan para responder (OAI-SearchBot, ChatGPT-User, PerplexityBot,
> Perplexity-User). Excelente = hay un robots.txt y los 4 pueden leer · notable = pueden leer, pero solo porque
> no hay robots.txt (política por omisión; convención) · baja = alguno · nula = ninguno. Los de entrenamiento no
> cuentan aquí: bloquearlos es una decisión, no un defecto.

Own grouping; the same parser as card 21 over a different agent list.

```sh
# same python3 parser as card 21, with the final list replaced by:
# "OAI-SearchBot ChatGPT-User PerplexityBot Perplexity-User"
```

Measured on the home page: **Excellent** · `4/4 · OAI-SearchBot: ✓ · ChatGPT-User: ✓ · PerplexityBot: ✓ ·
Perplexity-User: ✓` · "4 of 4 may read the root."

## 23. Sitemap — `sitemap`

Stable · sitemaps.org protocol 0.9 · `technical-seo` · v2026.10

> Excellent = declared in robots.txt, responding, a valid sitemap XML, includes this page and carries lastmod
> dates · strong = declared and valid, but misses this page or lastmod · good = responding and valid, not
> declared · low = responds but is not a valid sitemap · none = not found.

> Excelente = declarado en robots.txt, responde, es un XML de sitemaps válido, incluye esta página y lleva fechas
> lastmod · notable = declarado y válido, pero no incluye esta página o no lleva lastmod · buena = responde y es
> válido, sin declarar · baja = responde pero no es un sitemap válido · nula = no se encuentra.

```sh
curl -s 'https://zentimes.es/robots.txt' | grep -i '^sitemap:'; curl -s 'https://zentimes.es/sitemap.xml' | grep -o '<loc>' | wc -l
```

Measured on the home page: **Excellent** · `https://zentimes.es/sitemap.xml · 27 URLs` · "Valid sitemap."

## 24. /llms.txt — `llms`

Informational: no rhythm line, no label. The caveat is the whole text:

> Not rated, on purpose: having one is not a visibility lever. In a study of 137,210 domains, 97% of published
> llms.txt files got zero requests in a month. It is reported because it is asked about, not because it is
> recommended.

> Sin valorar, a propósito: tenerlo no es una palanca de visibilidad. En un estudio de 137.210 dominios, el 97%
> de los llms.txt publicados recibió cero peticiones en un mes. Se informa porque se pregunta, no porque se
> recomiende.

`needs-verification`: the page still does not link the study it cites.

Cookbook line: `llms_txt_bytes` · `04-technical/ai-crawler-access` · cross-checked 2026-09-23 (13:32Z) against
cookbook v0.1.4. **Same metric id as the cookbook** (UTF-8 bytes).

```sh
curl -s 'https://zentimes.es/llms.txt' | wc -c
```

Measured on the home page: `HTTP 200 · 9484 bytes` (`data-value="9484"`).

---

## Summary table

| # | card | `data-fact` | rhythm | evidence class | cookbook id | home page, 2026-09-23 14:44Z |
|---|---|---|---|---|---|---|
| 1 | HTTP status and time | `status` | Stable | technical-seo | — | Excellent |
| 2 | Transport security | `transporte` | Stable | security | — | Strong |
| 3 | Indexing directives | `indexacion` | Changing | technical-seo | — | Excellent |
| 4 | Mobile viewport | `viewport` | Stable | technical-seo | — | Excellent |
| 5 | Title and meta description | `titulo-descripcion` | Stable | technical-seo | — | Good |
| 6 | Declared language | `idioma` | Stable | technical-seo | — | Excellent |
| 7 | Words without JavaScript | `sin-js` | Changing | technical-seo | `words_visible_no_js` | Not rated (526) |
| 8 | JSON-LD nodes with @type | `jsonld-nodos` | Stable | aeo | `typed_entities` | Strong (9) |
| 9 | Distinct Schema.org types | `jsonld-tipos` | — | — | — | informational (5) |
| 10 | Publication and update dates | `fechas` | Changing | geo-precondition | — | **None** |
| 11 | Article authorship | `autoria` | Changing | geo-precondition | — | Not rated (no article) |
| 12 | Structured data completeness | `completitud` | Changing | aeo | — | Excellent (7/7) |
| 13 | sameAs links | `entidad` | Changing | geo-precondition | — (own rule) | Excellent |
| 14 | Canonical | `canonical` | Stable | technical-seo | — | Excellent |
| 15 | hreflang | `hreflang` | Stable | technical-seo | — | Excellent |
| 16 | Heading structure | `encabezados` | Changing | geo-precondition | — (same class as chunking, not its metric) | Excellent |
| 17 | Links to sections (#id) | `anclas` | Stable | geo-precondition | `unresolved_fragment_links` (not in v0.1.4; PR #32) | Excellent |
| 18 | Image alt text | `imagenes` | Stable | technical-seo | — | Excellent |
| 19 | Outbound links | `salientes` | Stable | technical-seo | — | Excellent (3) |
| 20 | Figures with a source | `cifras` | Changing | geo-precondition | — (anchoring idea; `needs-verification`) | Not rated (no percentages) |
| 21 | robots.txt — AI agents | `robots` | — | — | `ai_user_agents_allowed` | informational (8/8) |
| 22 | robots.txt — search assistants | `robots-busqueda` | Changing | technical-seo | — (own grouping) | Excellent (4/4) |
| 23 | Sitemap | `sitemap` | Stable | technical-seo | — | Excellent |
| 24 | /llms.txt | `llms` | — | — | `llms_txt_bytes` | informational (9484) |

Tallies from the served HTML (`class="insp-ritmo r-estable"` / `r-cambiante`, `span.insp-regla-v`,
`span.insp-nivel`): Stable 12 · Changing 9 · `technical-seo` 12 · `geo-precondition` 6 · `aeo` 2 · `security`
1 · labels on the home page: excellent 14, strong 2, good 1, none 1, not rated 3.

Three labels below excellent on the site's own home page in this run: transport (no `includeSubDomains`),
title/description (252-character description) and, new with this version, publication and update dates
(**None**: the home page declares no `dateModified` or `datePublished`). Whether they are still there when you
read this is a matter for a fresh run, not for this file.
