# Visible text of https://zentimes.es/tools/ai-inspector/?url=https://zentimes.es/
# Captured 2026-09-23 15:03Z with: curl -sL --compressed -A 'Mozilla/5.0 (compatible; ledger-capture/1.0)' 'https://zentimes.es/tools/ai-inspector/?url=https://zentimes.es/' (HTML tags, <script> and <style> removed, entities unescaped, whitespace normalised; no other edits)

AI citability inspector — facts, with the command that reproduces them
Zentimes
Menu
Services
GEO audit
GEO implementation
GEO monitoring
Web & technical SEO
All services →
Work
Cases
kenetg.com
In the open
Experiments
Open source
Resources
Notes
Lo que no se mide no cuenta: cómo se prueba que el GEO funciona
Cómo hacer que ChatGPT te cite: háblale en su idioma
GEO no es SEO (pero lo necesita por debajo)
All notes
Reference
Glossary
FAQ
Tools
Inspector
About
Contact
EN
/
ES
Zentimes
/
Inspector
Free tool
What an AI client actually sees on your page
Paste a URL. Each check comes with its rating, the rule that decides it and the command that reproduces it, so you never have to take my word for any of them.
Your page URL
Inspect
No overall score and no prediction. Each check gets a label with the exact rule behind it and the command to verify it. These are properties of your page, not a promise: no check measures whether an AI cites you, which is the engine's decision and nobody outside it controls.
https://zentimes.es/
Criteria v2026.11 · 2026-09-23
HTTP response
Excellent
200 · 25 ms
Returns 200 directly.
Stable · HTTP semantics and Google's redirect documentation
Excellent = the final URL returns 200, no meta refresh, 10 hops or fewer (Googlebot follows up to 10; http-network-errors, 3xx section) · good = 200, but the final HTML has a meta refresh (Google treats it as a redirect) · none = the final URL returns 4xx (except 403), 5xx, or more than 10 hops · not rated = 403 or 429: may be a block on the inspector; if Googlebot gets the same, it is serious. The number of hops does not decide the level: it depends on how the URL is pasted. HTTPS is rated under "transport".
technical-seo · v2026.11
The time is one measurement from one place and is not rated: network timing varies with where it is measured from.
Reproduce it
curl -sIL 'https://zentimes.es/' | grep -iE '^(HTTP/|location:)'
Transport security (HTTPS and HSTS)
Strong
max-age=31536000
HSTS max-age=31536000.
How to improve it
Add Strict-Transport-Security: max-age=31536000; includeSubDomains, and load scripts, styles, iframes and forms over https://.
Stable · RFC 6797 and the HSTS preload list requirements
Security, not legibility. Excellent = HTTPS, HSTS of at least one year with includeSubDomains, and no script, stylesheet, iframe or form over http:// · strong = HSTS of at least one year without includeSubDomains, no insecure dependencies · good = HSTS under one year · low = HTTPS without HSTS, or some script, stylesheet, iframe or form over http:// (Google may not prefer the HTTPS version as canonical) · none = no HTTPS. The year and includeSubDomains come from hstspreload.org; preload is not required (the page itself does not recommend it). Images over http:// are reported and do not lower the label (Google excludes images from "insecure dependencies").
security · v2026.11
Reproduce it
curl -sI 'https://zentimes.es/' | grep -i '^strict-transport-security:'
robots.txt — search engines and assistants
Excellent
7/7 · Googlebot ✓ · Bingbot ✓ · Applebot ✓ · OAI-SearchBot ✓ · Claude-SearchBot ✓ · Claude-User ✓ · PerplexityBot ✓
7 of 7 allowed on this path.
Changing · the agent list and their roles change every few months
On THIS page's PATH, for Googlebot, Bingbot, Applebot, OAI-SearchBot, Claude-SearchBot, Claude-User, PerplexityBot (Applebot without its own group inherits Googlebot's). Excellent = robots.txt returns 200, UTF-8, with those groups within the first 500 KiB, allows them this path and does not block the page's JS/CSS to Googlebot or Applebot · strong = allowed but only by omission (4xx except 429), or blocks the page's JS/CSS to Googlebot or Applebot, or not UTF-8, or some group sits past 500 KiB · low = Googlebot allowed, but one of the other 6 blocked · none = Googlebot blocked (not eligible for AI Overviews or AI Mode), all 7 blocked, or robots.txt returns 5xx or 429 · not rated = no response (may be an inspector failure). Sources: RFC 9309; Google's robots.txt spec (4xx except 429 = no file; content past 500 KiB is ignored; on 5xx it stops crawling for 12 h and uses the last good copy up to 30 days). Treating 429 as 5xx is an INFERENCE from two Google pages (robots.txt and http-network-errors), not from the RFC. Conventions: including Bingbot; "no robots.txt" = strong; Claude-User's weight. The rule says "allowed by robots.txt", not "can read".
technical-seo · v2026.11
Reproduce it
curl -sf 'https://zentimes.es/robots.txt' | python3 -c 'import sys,re
G={};C=[];S=0
for l in sys.stdin:
l=l.split("#")[0].strip()
if ":" not in l: continue
f,_,v=l.partition(":");f=f.strip().lower();v=v.strip()
if f=="user-agent":
C=C if S else [];C.append(v.lower());G.setdefault(v.lower(),[]);S=1
elif f in("allow","disallow"):
S=0
for a in C:G[a].append((f,v))
def m(p):
e=p.endswith("$");b=p[:-1] if e else p
return re.match("".join(".*" if c=="*" else re.escape(c) for c in b)+("$" if e else ""),"/")
def ok(u):
R=G.get(u.lower(),G.get("*"))
if R is None:return 1
L,A=-1,1
for d,v in R:
if v=="":
if d!="disallow":continue
n,x=0,1
elif m(v):n,x=len(v),d=="allow"
else:continue
if n>L or(n==L and x):L,A=n,x
return A
print(sum(ok(u) for u in "Googlebot Bingbot Applebot OAI-SearchBot Claude-SearchBot Claude-User PerplexityBot".split()))'
robots.txt — training and user agents (informative)
Not rated
8/8 · GPTBot ✓ · ClaudeBot ✓ · anthropic-ai ✓ · PerplexityBot ✓ · Google-Extended ✓ · CCBot ✓ · Applebot-Extended ✓ · Bytespider ✓
Training: GPTBot ✓ · ClaudeBot ✓ · anthropic-ai ✓ · Google-Extended ✓ · Applebot-Extended ✓ · CCBot ✓ · Bytespider ✓. User: ChatGPT-User ✓ · Perplexity-User ✓.
Changing · training and user agents change name and role often
Informative, never rated: blocking these agents is a decision, not a defect. GPTBot, ClaudeBot, CCBot and Bytespider crawl for training; Google-Extended controls Gemini training and grounding in some Google systems, and per Google does not affect inclusion in Search (that it does not affect AI Overviews either is an inference: AI Overviews requires indexing, which Googlebot governs); Applebot-Extended does not crawl, it only controls use. ChatGPT-User and Perplexity-User act on a user's request and their provider says robots.txt may not apply. Anthropic advises against blocking by IP because it prevents reading robots.txt.
technical-seo · v2026.11
The data-value is the recipe's metric: the eight tokens on the root path (/). Google-Extended governs training and grounding only — it does not affect Google Search.
ai_user_agents_allowed
04-technical/ai-crawler-access · cross-checked 2026-09-23 (13:32Z) against cookbook v0.1.4
Reproduce it
curl -sf 'https://zentimes.es/robots.txt' | python3 -c 'import sys,re
G={};C=[];S=0
for l in sys.stdin:
l=l.split("#")[0].strip()
if ":" not in l: continue
f,_,v=l.partition(":");f=f.strip().lower();v=v.strip()
if f=="user-agent":
C=C if S else [];C.append(v.lower());G.setdefault(v.lower(),[]);S=1
elif f in("allow","disallow"):
S=0
for a in C:G[a].append((f,v))
def m(p):
e=p.endswith("$");b=p[:-1] if e else p
return re.match("".join(".*" if c=="*" else re.escape(c) for c in b)+("$" if e else ""),"/")
def ok(u):
R=G.get(u.lower(),G.get("*"))
if R is None:return 1
L,A=-1,1
for d,v in R:
if v=="":
if d!="disallow":continue
n,x=0,1
elif m(v):n,x=len(v),d=="allow"
else:continue
if n>L or(n==L and x):L,A=n,x
return A
print(sum(ok(u) for u in "GPTBot ClaudeBot anthropic-ai PerplexityBot Google-Extended CCBot Applebot-Extended Bytespider".split()))'
Indexing directives (per engine)
Excellent
—
Google: Excellent (—) · Bing: Excellent (—).
Changing · how each engine applies its directives to AI keeps changing (the Bing source is from 2023)
One result for Google and one for Bing; the label is the worse. Excellent = nothing restrictive · strong = data-nosnippet over less than half the main text · good = max-snippet between 1 and 159, or nocache for Bing (in Copilot, only URL, title and snippet) · low = nosnippet or max-snippet:0, noarchive for Bing (out of Copilot answers), or data-nosnippet over half or more of the main text · none = noindex or none, or a past unavailable_after (Google). Sources: Google's robots-meta-tag and ai-features; Bing's September 2023 announcement. Conventions: the Bing source does not mention nosnippet or max-snippet (applied anyway), the 160-character cut and the 50%. noarchive and nocache for Google are reported: Google no longer uses them. A noindex in the initial HTML counts even if a script removes it later.
technical-seo · v2026.11
Reproduce it
curl -sI 'https://zentimes.es/' | grep -i '^x-robots-tag:'; curl -s 'https://zentimes.es/' | grep -oiE '<meta[^>]+name="(robots|googlebot|bingbot)"[^>]*>|data-nosnippet'
HTML size
Excellent
48 KB
48 KB.
Changing · it is a Google implementation limit, subject to change
Excellent = 2 MB or less of uncompressed HTML · low = over 2 MB: what is past the cut is not indexed. The 2 MB limit is on the Googlebot page dated 2026-02-03 (that same page's machine summary still says 15 MB). That exceeding it is low and not none is our choice.
technical-seo · v2026.11
Reproduce it
curl -s 'https://zentimes.es/' | wc -c
Character encoding
Excellent
utf-8
BOM: — · header: utf-8 · meta: utf-8.
Stable · HTML Standard and W3C recommendation
Excellent = UTF-8 declared in at least one way (BOM, Content-Type header or <meta charset> in the first 1024 bytes) with no contradictions · strong = declared and consistent, but not UTF-8 · low = undeclared, <meta charset> after 1024 bytes, or contradictory declarations. Precedence BOM > header > meta (HTML Standard). Convention: the severity order; the W3C recommends UTF-8 and sets the 1024 bytes.
technical-seo · v2026.11
Reproduce it
curl -sI 'https://zentimes.es/' | grep -i '^content-type:'; curl -s 'https://zentimes.es/' | head -c 1024 | grep -oiE '<meta[^>]*charset[^>]*>'
Head readable by Google
Excellent
—
Head with no invalid elements.
Stable · Google's documentation on valid metadata
Google reads <head> up to the first element that is not title, meta, link, script, style, base, noscript or template; what follows is ignored. Excellent = no invalid element in <head> · strong = there is one, but no metadata after it · low = there is one and metadata sits after it. Convention: that an invalid element with no metadata after it is strong, and the source-order walk.
technical-seo · v2026.11
Reproduce it
curl -s 'https://zentimes.es/' | sed -n '/<head/,/<\/head>/p' | grep -oE '<[a-zA-Z][a-zA-Z0-9-]*' | sort | uniq -c
Words present without running JavaScript
Not rated
526
526 words.
Changing · if AI crawlers start running JavaScript, the rule changes
Only the clear case is flagged: none = under 50 words without running JavaScript, the page arrives nearly empty. Above that the number is reported unrated: no data ties a word count to an outcome. Basis: the main AI crawlers do not run JavaScript (Vercel/MERJ study, Dec 2024, with the agent list). Convention: the cut at 50.
technical-seo · v2026.11
words_visible_no_js
04-technical/ssr-vs-csr-rendering · cross-checked 2026-09-23 (13:32Z) against cookbook v0.1.4
Reproduce it
curl -s 'https://zentimes.es/' | perl -0777 -pe 's/<script.*?<\/script>//gis; s/<style.*?<\/style>//gis; s/<!--.*?-->//gis; s/<[^>]+>/ /g; s/&#?[a-z0-9]+;/ /gi; s/\s+/ /g; s/^\s+|\s+$//g' | wc -w
Crawlable links
Excellent
53/53
53 of 53 crawlable with text (100%).
Stable · Google's documentation on crawlable links
A crawlable link is an <a> with a resolvable href (not empty, not just "#", not "javascript:") and anchor text (its text or the linked image's alt). Not crawlable: an <a> with onclick and no href, <a routerLink>, a <span href>. Excellent = all crawlable and with text · strong = 95% or more · good = 80% or more · low = under 80% · none = no crawlable <a href> while script links exist · not rated = no links. Source: Google's documentation on crawlable links. Conventions: the 95% and 80% cuts; aria-label and title do not count as text (Google only documents text and alt).
technical-seo · v2026.11
Reproduce it
curl -s 'https://zentimes.es/' | grep -oiE '<a [^>]*>' | head -50
Hidden text (informative)
Not rated
0 %
0% of the main text hidden, in 2 block(s).
Changing · depends on how each system renders
Informative: measures main text inside elements with hidden, aria-hidden="true", style display:none or visibility:hidden, or a closed <details>. Not rated: there is no evidence-based threshold and some hiding is legitimate, like menus. CSS classes are not resolved without rendering.
geo-precondition · v2026.11
Reproduce it
curl -s 'https://zentimes.es/' | grep -oiE '<[a-z]+[^>]*(hidden|aria-hidden="true"|display: *none|visibility: *hidden)[^>]*>'
Title, description and h1
Strong
Zentimes — GEO & AI Search Visibility
Generative Engine Optimization (GEO): I build the structure that makes your brand citable in AI answers — ChatGPT, Perplexity, Google AI Overviews — backed by technical SEO and security engineering. And I prove it: dogfooded and measured, not promised.
Title (37 ch.): "Zentimes — GEO & AI Search Visibility". Description: 252 ch. h1: "I make your brand citable by AI — and I publish the proof.". Missing: an h1 that reflects the title.
How to improve it
A <title> saying what the page is (not just the site name), consistent with the h1, and a <meta name="description"> summing it up.
Stable · Google's documentation on title links and snippets
From the effective head. Excellent = non-empty title and description, the title is not just the site name and, if there is an h1, it reflects the title (one contains the other or they share at least half their words) · strong = both, but the h1 does not reflect the title or the title is just the site name · low = only one · none = neither. With no h1, that coherence is not rated. Source: Google's title-link documentation (the h1 is one source of the title link; rewrite causes like a "half-empty title" or a duplicated site name) and snippet documentation (the description has no length limit). Conventions: the 50% word share and the normalization. Lengths, og:title and og:description are reported, they do not decide.
technical-seo · v2026.11
Reproduce it
curl -s 'https://zentimes.es/' | grep -oE '<title[^>]*>[^<]*</title>|<meta[^>]+name="description"[^>]*>|<h1[^>]*>[^<]*'
Heading hierarchy
Excellent
h1: 1 · h2: 5
1 h1 · 5 h2 · 0 skips · 0 empty.
Stable · HTML Living Standard
Excellent = at least one h1, no skipped level (a heading more than one level below the previous, like an h3 right after an h1) and no empty heading · strong = h1 and no skips, but an empty heading · good = h1, but with skips · low = headings, but no h1 · none = no headings. Several h1 do not lower it: the HTML Standard allows them. No number of h2 is required. Convention: the whole scale (the standard does not require an h1).
technical-seo · v2026.11
Reproduce it
curl -s 'https://zentimes.es/' | perl -0777 -pe 's/<script.*?<\/script>//gis; s/<!--.*?-->//gs' | grep -oiE '<h[1-6][ >]' | tr -d '< >'
Canonical URL
Excellent
https://zentimes.es/
One, absolute, to this same page.
Stable · Google's canonicalization documentation
Excellent = a single canonical, inside the effective head, absolute, pointing to this page and matching the Link header if any · strong = points to this page, but with a relative path (Google recommends absolute) · good = no canonical (not required: Google chooses) or points to another URL (may be intended) · low = several different canonicals, HTML/header contradiction, or a canonical outside the effective head (Google ignores it). Source: Google's canonicalization documentation. Convention: that "no canonical" and "another URL" are good. og:url is not required by Google: reported.
technical-seo · v2026.11
Reproduce it
curl -sI 'https://zentimes.es/' | grep -i '^link:'; curl -s 'https://zentimes.es/' | grep -oE '<link[^>]*rel="canonical"[^>]*>'
Language versions (hreflang)
Excellent
en, es, x-default
3 values, with x-default · 1 of 1 versions link back.
Stable · Google's documentation on localized versions
Only if the page declares hreflang (HTML or Link header). Excellent = valid codes, self-reference, absolute URLs with protocol, inside the effective head, and every checked version (up to 5) links back with its canonical pointing to itself · good = no self-reference, or some version does not link back, returns 404/410 or has its canonical elsewhere (that pair is ignored) · low = invalid codes, relative URLs or annotations past the head cut · none = no checked version links back · not rated = no hreflang, or no version checkable. x-default is reported: Google recommends it, does not require it. Source: Google's localized-versions documentation (return links are required). Conventions: the cap of 5 and that 403/429/999 do not lower it.
technical-seo · v2026.11
Reproduce it
curl -sI 'https://zentimes.es/' | grep -i '^link:'; curl -s 'https://zentimes.es/' | grep -oE '<link[^>]*hreflang="[^"]*"[^>]*>'
Declared language (<html lang>)
Excellent
en
lang="en" · og:locale en_US.
Stable · WCAG 3.1.1
Excellent = <html lang> present and valid (BCP 47 tag) · low = malformed · none = missing. Not a Google signal (Google does not use lang to detect language): rated for accessibility, WCAG 3.1.1 (level A). og:locale is reported. Convention: the regex validation approximates BCP 47.
accessibility · v2026.11
Reproduce it
curl -s 'https://zentimes.es/' | grep -oE '<html[^>]*>'
Viewport and zoom
Excellent
width=device-width, initial-scale=1
viewport: width=device-width, initial-scale=1
Stable · WCAG 1.4.4 and W3C ACT rule b4f0c3
Excellent = width=device-width and zoom allowed · good = viewport without width=device-width, zoom allowed · low = no viewport · none = zoom blocked: user-scalable=no/0 or maximum-scale below 2 (fails WCAG 1.4.4 AA per W3C ACT rule b4f0c3). Convention: the level order; missing width=device-width is not a confirmed WCAG failure. No source says AI engines use it.
accessibility · v2026.11
Reproduce it
curl -s 'https://zentimes.es/' | grep -oiE '<meta[^>]+name="viewport"[^>]*>'
Sitemap
Strong
https://zentimes.es/sitemap.xml
Valid sitemap. Missing: lastmod.
How to improve it
A sitemap.xml with your canonical pages, each with a <lastmod> including date and time, declared with "Sitemap:" in robots.txt.
Stable · sitemaps.org protocol 0.9 and Google and Bing documentation
Excellent = declared in robots.txt, valid XML, within limits (50,000 URLs and 50 MB), absolute same-host URLs, includes this page's canonical and its lastmod has an ISO 8601 date and time (W3C Datetime format, sitemaps.org) · strong = valid, but the page is missing, a variant appears, its lastmod is missing or date-only, or it is not declared in robots.txt · low = not a valid sitemap, over the limits, not UTF-8, or relative or other-host URLs · not rated = no sitemap (Google does not need one on small, well-linked sites) or not checkable. For an index, up to 5 children are opened. Conventions: the cap of 5, that not declaring it lowers to strong (Search Console submissions are not visible) and that a date-only lastmod is strong (Bing recommends it).
technical-seo · v2026.11
Reproduce it
curl -s 'https://zentimes.es/robots.txt' | grep -i '^sitemap:'; curl -s 'https://zentimes.es/sitemap.xml' | grep -o '<loc>' | wc -l
Structured data and entity (JSON-LD nodes with @type)
Strong
9
9 typed nodes. Missing: name, url and logo or image on the entity.
How to improve it
On the home page, JSON-LD with your entity (name, url and a logo of at least 112×112 px) and a WebSite with name and url.
Stable · Schema.org vocabulary and Google documentation
On the home page: excellent = no invalid block, an entity (Organization, Person, LocalBusiness…) with name, url and logo or image (if Organization, a logo that responds and is at least 112×112 px) and a WebSite with name and url · strong = entity, but no WebSite or a failed logo · good = types, but no entity · low = only invalid blocks, or the home with no structured data in any format. Off the home page: excellent = at least one typed node and no invalid block; no structured data = not rated (not required). Only Microdata/RDFa = not rated (format not read). Sources: Google's documentation (logo ≥112×112, WebSite on the home page). Convention: requiring name, url and logo on the entity (Google recommends them). Rated as Search eligibility: Google says structured data is not required for generative search.
aeo · v2026.11
Counting nodes is not validating them. Google says structured data is not required for generative AI search: this is rated as search eligibility.
typed_entities
04-technical/structured-data-jsonld · cross-checked 2026-09-23 (13:32Z) against cookbook v0.1.4
Reproduce it
curl -s 'https://zentimes.es/' | grep -o '"@type":' | wc -l
Distinct Schema.org types present
Person, PostalAddress, ProfessionalService, Service, WebSite
Reproduce it
curl -s 'https://zentimes.es/' | grep -o '"@type":"[^"]*"' | sort -u | wc -l
Properties per type
Strong
1 nodes of documented types
1 of 1 missing a recommended: Organization missing logo. 8 of 9 nodes are of types with no documented list (Person, PostalAddress, WebSite, Service) and are not rated.
How to improve it
Complete each node with its properties: a half-filled node declares a type and withholds what that type needs.
Changing · property lists change with Google's documentation
Only types with properties documented by Google. Required: Product (name and offers, aggregateRating or review), BreadcrumbList (itemListElement, and in each item position, name and item, optional on the last), ProfilePage (mainEntity with name). Recommended: Article, BlogPosting and NewsArticle (headline, datePublished, dateModified, author and image, which must respond and be at least 50,000 pixels) and Organization when it is the main entity (name, url, logo, sameAs). Excellent = no node missing required or recommended · strong = required complete and 1 node missing a recommended · good = required complete and 2 or more nodes missing recommended · low = some node missing a required one (not eligible for its rich result) · not rated = no documented type. Conventions: the Organization subset, the cap of 2 checked images and the cut between 1 and 2 nodes. Other types are reported.
aeo · v2026.11
Reproduce it
curl -s 'https://zentimes.es/' | grep -oE '<script[^>]+application/ld\+json[^>]*>.*</script>' | head -c 4000
Structured data visible on the page
Not rated
—
No checkable main entity.
Stable · Google's general structured data policy
Structured data must describe what the page shows (Google's general policy). The main entity is checked: an article's headline and author.name; a Product's name and price; each value is looked for in the visible text without JavaScript. Excellent = all appear · strong = 1 missing · good = several missing, but fewer than half · low = half or more missing · not rated = no checkable main entity. Conventions: what the main entity is, the normalization and the cuts.
technical-seo · v2026.11
Reproduce it
curl -s 'https://zentimes.es/' | grep -oE '"(headline|name|price)":"[^"]*"'
Your entity's identity links (sameAs)
Excellent
https://github.com/ferinazumaDEV
https://www.linkedin.com/in/fernando-aporta-franco-340219230/
2 sameAs: 1 respond, 0 missing, 1 uncheckable.
Changing · how engines use entities is still being studied
Excellent = every checkable sameAs responds, at least one checked, all HTTPS and no repeats · strong = they respond, but some use http:// or repeat · low = some return 404/410, or the entity has no sameAs · not rated = no entity (rated by the structured-data card) or none checkable. The first 8 are checked; 403, 429 and 999 count as uncheckable. Wikidata and quantity are not rewarded, and "same entity" cannot be measured. Conventions: the cap of 8, requiring HTTPS and no repeats, and that missing sameAs is low (Google recommends, does not require it).
geo-precondition · v2026.11
Reproduce it
curl -s 'https://zentimes.es/' | grep -oE '"sameAs":(\[[^]]*\]|"[^"]*")'
Article dates
Not rated
— · —
Not an article.
Stable · Google's date and Article documentation; freshness as a factor is left out on purpose
Articles only (Article, BlogPosting, NewsArticle, og:type=article or article:published_time). Excellent = structured ISO 8601 date with time and zone, not future, dateModified not before datePublished, and a visible labelled date ("Published", "Updated"…) matching the same day · strong = structured and visible, but no time zone or the visible one unlabelled · good = only structured or only visible · low = malformed or future date, dateModified before datePublished, or visible and structured contradicting · none = article with no date · not rated = not an article. Source: Google's date and Article documentation (time zone recommended). Conventions: article detection, the label list, the 24 h margin and same-day matching. Freshness as a factor is left out on purpose.
technical-seo · v2026.11
Reproduce it
curl -s 'https://zentimes.es/' | grep -oE '"(datePublished|dateModified)":"[^"]*"|<meta[^>]+property="article:(published|modified)_time"[^>]*>|<time[^>]*>'
Article authorship
Not rated
—
Not an article.
Stable · Google's Article documentation and helpful-content guide
Articles only. Excellent = author of @type Person or Organization, name with just the name and one author per field, a url or sameAs returning 200, and the name visible on the page · strong = name visible, but no url or sameAs, a non-responding link, a "dirty" name (prefix, title, several authors) or another @type · good = only a visible byline with no structured author, or a structured author with no visible byline · none = article with no author · not rated = not an article. Source: Google's Article documentation and helpful-content guide. Conventions: byline detection by text match and dirty-name detection by patterns.
technical-seo · v2026.11
Reproduce it
curl -s 'https://zentimes.es/' | grep -oE '"author":[^]}]*'
Owner and contact
Excellent
2/2
3 of 3.
Stable · a principle of Google's quality guidelines
Links in the initial HTML to an "about" page and a contact page, and an owner (publisher or Organization) in JSON-LD. Excellent = all three · strong = two · good = one · none = none. Not a ranking signal: a trust criterion from Google's quality guidelines (who is behind it and how to reach them). Conventions: the URL and text patterns, and the count-based scale.
technical-seo · v2026.11
Reproduce it
curl -s 'https://zentimes.es/' | grep -oiE 'href="[^"]*(about|sobre|nosotros|contact|contacto)[^"]*"'
Image alt text
Not rated
—
1 image(s) with alt="" (decorative).
Stable · WCAG 1.1.1 and Google Images documentation
Useful = non-empty alt, not a file name, 125 characters or fewer and not repeated across different images. alt="" (decorative) images are listed apart and do not count, unless they are a link's only content (then the link has no text). An image with no real src (empty or data:) that only loads via JavaScript counts as not useful. Excellent = all useful · strong = 90% or more · good = 70% or more · low = less · none = none · not rated = no images, or all alt="" outside links. Basis: WCAG 1.1.1 and Google Images (alt is the anchor text of a linked image; stuffing alt with keywords is spam). Conventions: 90%, 70%, 125 characters and the repeat rule.
accessibility · v2026.11
Reproduce it
curl -s 'https://zentimes.es/' | grep -oiE '<img[^>]*>'
Links to sections of this page (#id)
Excellent
1
1 links, all resolve, no repeated ids.
Stable · HTML Standard
Links from this page to one of its own sections (#id; "#" and "#top" are the top of the page). Excellent = all reach an id that exists exactly once and no id repeats on the page (the HTML Standard requires unique ids) · strong = they resolve, but some ids repeat · low = some lead nowhere · not rated = no such links. Same rule as the cookbook's unresolved_fragment_links, applied to the whole page.
geo-precondition · v2026.11
Reproduce it
curl -s 'https://zentimes.es/' | grep -oE 'href="#[^"]*"' | sort -u
Outbound links (informative)
Not rated
3
3 external links · 3 checked: 2 respond, 0 broken, 1 uncheckable. Uncheckable: https://www.linkedin.com/in/fernando-aporta-franco-340219230/.
Stable · HTTP semantics
Informative: up to 10 links to other domains are requested, reporting which respond, which return 404/410 and which cannot be checked (403, 429, 999). Not rated: no source backs a scale for this.
technical-seo · v2026.11
Reproduce it
curl -s 'https://zentimes.es/' | grep -oE 'href="https?://[^"]*"' | sort -u
Figures with a source
Not rated
—
No percentages in the text.
Stable · a principle of Google's helpful-content guide ("clear sourcing")
Paragraphs and list items with a percentage. They have a source if they carry an http(s) link in the same paragraph or its containing block (a card), any domain, or a #anchor note whose target holds a link; as in the cookbook's anchoring recipe. Excellent = all sourced · strong = 80% or more · good = 50% or more · low = less · not rated = no percentages. Basis: Google's helpful-content guide asks for "clear sourcing". Conventions: 80% and 50%, limiting to percentages and the block scope.
geo-precondition · v2026.11
Reproduce it
curl -s 'https://zentimes.es/' | grep -oE '[0-9]+([.,][0-9]+)? ?%'
/llms.txt (informative)
Not rated
HTTP 200 · 9484 bytes
Exists (9484 bytes).
Changing · a proposal with no documented adoption by engines
Informative: having one raises no label and lacking one lowers none. Google's guide says such files neither help nor hurt, and there is no documented adoption by engines. Reported because people ask.
technical-seo · v2026.11
llms_txt_bytes
04-technical/ai-crawler-access · cross-checked 2026-09-23 (13:32Z) against cookbook v0.1.4
Reproduce it
curl -s 'https://zentimes.es/llms.txt' | wc -c
This looks at one page. The mini-audit applies the same criteria to the pages that matter on your site and tells you what to fix first.
Ask for the mini-audit
Criteria v2026.11
This is a snapshot of today. Checks marked Stable (crawling, robots.txt, canonical, structured data) have held for years. Checks marked Changing are the part the field is still working out: when we learn something new, the rule changes and this history says so, with the date and the reason. That is why a label can change even if your site has not.
Criteria history
v2026.11
· 2026-09-23 — Rules rebuilt from verified evidence (64 sources confirmed by a second agent and reviewed by the GEO agent). Labels that move from v2026.10, and why: one h1 and a number of h2 are no longer required (HTML allows several h1); x-default is no longer required (Google recommends it, does not require it); og:url and title/description lengths no longer decide the level (Google does not ask for them; the description has no limit); redirects from the pasted URL no longer count (they depend on how it is pasted; Googlebot follows up to 10); having no sitemap becomes "not rated" (Google does not need one on small, well-linked sites); HSTS preload is not required (hstspreload.org does not recommend it); "no entity" becomes "not rated" on inner pages; robots.txt is evaluated on the page's path and for the 7 agents that decide appearing in search and answers, and training policy becomes informative; indexing is computed per engine (Google and Bing). Classes: language, viewport and images move to accessibility (not a cookbook class). New: HTML size, encoding, valid head, crawlable links, hidden text (informative), visible structured data, and owner and contact. Outbound links and llms.txt become informative.
v2026.10
· 2026-09-23 — Closes the gaps that let incomplete pages score "excellent": indexing (noindex, nosnippet, X-Robots-Tag, which overrides everything), viewport, dates, article authorship, minimum properties per structured-data type, broken outbound links and figures with a source. The criteria now carry a review date: if nobody reviews them within 45 days, deployment fails.
v2026.09.2
· 2026-09-23 — HSTS moves out of "response" into its own check, "transport". It is a security practice, not a legibility one: it could not lower the HTTP response label. Flagged by the GEO agent reviewing v2026.09.1.
v2026.09.1
· 2026-09-23 — Bar raised: "excellent" now means nothing verifiable is left to improve in that area (HSTS, Open Graph, og:locale, hreflang return links, sitemap listing this page with lastmod, linked JSON-LD graph, heading hierarchy without skips, unique ids). New check: image alt text. Words without JavaScript and sameAs count were not tightened: no data backs a higher bar.
v2026.09
· 2026-09-23 — First version: one label per check with its rule published, no overall score. Reviewed before release by the GEO agent against the cookbook.
© 2026 Zentimes · ferinazumaDEV — Fernando Aporta Franco
Notes
Experiments
Open source
Inspector
FAQ
Glossary
Legal notice
llms.txt
GitHub
LinkedIn
